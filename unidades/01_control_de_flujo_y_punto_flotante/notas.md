# Unidad 01 — Control de flujo y aritmética de punto flotante

## Temas
- **Python:** `if` / `elif` / `else`, operadores de comparación y
  lógicos, `while`, `for`, `break` / `continue`.
- **Física computacional:** representación de números en punto
  flotante (IEEE 754), error de redondeo, por qué `0.1 + 0.2 != 0.3`,
  comparación de flotantes con una tolerancia (`abs(a - b) < eps`).

## Contenido
_Pendiente — código y notas de esta unidad._

## Datos
Las salidas de las simulaciones/experimentos de esta unidad se
guardan en [`datos/`](datos/).
