"""Herramientas sobre la representación de punto flotante.

Temas que corresponden a la unidad 01 (aritmética de punto flotante):

- epsilon_maquina(): calcular el épsilon de la máquina.
- son_iguales(a, b, tolerancia=...): comparar dos flotantes con una
  tolerancia, en vez de usar `==` directamente.
"""
